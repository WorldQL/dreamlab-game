{
  "name": "@dreamlab.gg/app-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build -- --watch",
    "build": "tsup",
    "lint": "prettier --check . && eslint src --ext mjs,js,ts,tsx",
    "format": "prettier --write . && eslint src --ext mjs,js,ts,tsx --fix",
    "fmt": "npm run format",
    "test": "npm run lint"
  },
  "devDependencies": {
    "@luludev/eslint-config": "^0.4.2",
    "@types/express": "^4.17.17",
    "eslint": "^8.45.0",
    "prettier": "^3.0.0",
    "tsup": "^7.1.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@luludev/env": "^0.6.5",
    "express": "^4.18.2"
  }
}
