{
  "name": "dreamlab-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "prettier --check . && eslint src --ext mjs,js,ts,tsx",
    "format": "prettier --write . && eslint src --ext mjs,js,ts,tsx --fix",
    "fmt": "npm run format",
    "test": "npm run lint"
  },
  "devDependencies": {
    "@lolpants/eslint-config": "^0.4.1",
    "eslint": "^8.43.0",
    "prettier": "^2.8.8",
    "typescript": "^5.0.2",
    "vite": "^4.3.9"
  },
  "dependencies": {
    "@dreamlab.gg/core": "^0.0.6"
  }
}
